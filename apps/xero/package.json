{
  "name": "@teejay/xero",
  "version": "0.0.1",
  "scripts": {
    "start": "pnpm build && pnpm serve",
    "build": "esbuild `find src -name '*.ts'` --outdir=dist --format=cjs",
    "serve": "node dist/index.js"
  },
  "devDependencies": {
    "@teejay/tsconfig": "workspace:^0.0.0",
    "esbuild": "^0.15.5",
    "puppeteer": "^18.0.5",
    "typescript": "^4.8.4"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.186.0",
    "dotenv": "^16.0.2",
    "fastify": "^4.5.3",
    "zod": "^3.18.0"
  }
}
